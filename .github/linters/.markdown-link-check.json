{
    "ignorePatterns": [
        {
            "pattern": "^(http|https)://localhost"
        },
        {
            "pattern": "^#"
        },
        {
            "pattern": "^https://github.com/Safe-DS/Runner/blob/main"
        }
    ]
}
